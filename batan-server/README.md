# Batan - Server
Remember to run this first:
$ npm install

Do NOT add/push .env file to git/github - the credentials are confidential.

## init dev
Run ts watch server:
$ npm run dev

## build
$ npm run tsc

## serve production
(Builds & runs)
$ npm run prod
