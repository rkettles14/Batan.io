export enum vertexStatus {
  open,
  settlement,
  city,
  blocked,
}

export enum player {
  none,
  red,
  white,
  blue,
  orange,
}

export enum resourceType {
  none,
  sheep,
  wheat,
  wood,
  ore,
  brick,
}

export enum developmentType {
  knight,
  monopoly,
  roadBuilder,
  victoryPointCard,
  yearOfPlenty,
}
